package lexer;

public enum TokenType {
    EOL,
    EOF,
    BINARY_OPERATOR,
    BOOLEAN_OPERATOR,
    INTEGER,
    FLOAT,
    IDENTIFIER,
    EQUALS,
    FUNCTION,
    OPEN_PARAN,
    CLOSE_PARAN,
    OPEN_BLOCK,
    CLOSE_BLOCK,
    OPEN_BRACKET,
    CLOSE_BRACKET,

    STRING,
    LET,
    CONST,
    RETURN,
    BOOLEAN,
    NULL,
    IF,
}
