package lexer;

public enum TokenType {
    EOL,
    EOF,
    BINARY_OPERATOR,
    BOOLEAN_OPERATOR,
    NUMBER,
    IDENTIFIER,
    EQUALS,
    FUNCTION,
    OPEN_PARAN,
    CLOSE_PARAN,
    OPEN_BLOCK,
    CLOSE_BLOCK,
    STRING,
    LET,
    CONST,
    RETURN,
    BOOLEAN,
    NULL,
    IF,
}
