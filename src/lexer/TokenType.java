package lexer;

public enum TokenType {
    EOL,
    EOF,
    BINARY_OPERATOR,
    NUMBER,
    IDENTIFIER,
    LiteralType,
    EQUALS,
    FUNCTION,
    OPEN_PARAN,
    CLOSE_PARAN,
    OPEN_BLOCK,
    CLOSE_BLOCK
}
